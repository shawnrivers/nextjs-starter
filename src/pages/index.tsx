import { Heading } from 'app/components/Heading';
import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <main>
      <Heading />
    </main>
  );
};

export default Home;
